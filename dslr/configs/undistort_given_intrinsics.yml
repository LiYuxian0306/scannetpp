# folder where the data is downloaded
data_root: DATA_ROOT

# splits: [nvs_sem_train, nvs_sem_val]
scene_ids: [05993dba6f]

# The following paths should be given relative to the DATA_ROOT/dslr/
# If not given, will use the default directory defined in ScannetppScene class

# input_image_dir: resized_images
# input_mask_dir: resized_anon_masks
# input_transforms_path: nerfstudio/transforms.json

downscale_factor: 1.0

# Optional: Custom output root directory (for read-only input directories)
# If not specified, outputs will be written to DATA_ROOT/data/{scene_id}/dslr_undistorted_by_iphone/
# If specified, outputs will be written to output_root/data/{scene_id}/dslr_undistorted_by_iphone/
# Example: output_root: /path/to/writable/directory
# output_root: null

# All use the relative path to the output base directory
out_image_dir: resized_images
out_mask_dir: resized_anon_masks
out_transforms_path: nerfstudio/transforms.json
output_root: /home/kylin/lyx/project_study/ExCap3D/data/dslr

